name: CI Trigger Isolation

on:
  push:

jobs:
  trigger-proof:
    runs-on: ubuntu-latest
    steps:
      - name: Prove push event reached Actions
        run: |
          echo "TRIGGER_PROOF=PASS"
          echo "SHA=${GITHUB_SHA}"
          echo "REF=${GITHUB_REF}"
